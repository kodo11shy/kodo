# 体验版逐页验收清单

> 日期：2026-06-18 | 阶段：体验版优化与收口 | 原则：不新增大功能，逐页验收
>
> ## 本轮修复记录（2026-06-18）
>
> | 编号 | 修复内容 | 涉及文件 | 验证 |
> |------|----------|----------|------|
> | F-001 | [P0] 老师工作台 API 失败时不再显示假数据 `totalCount: 20`，改为 0 + 错误提示条 | `teacher/dashboard/dashboard.js`, `.wxml`, `.wxss` | ✅ JS 语法通过 |
> | F-002 | [P0] 作业详情页移除冗余的 `FIXED_HOMEWORK_SUBJECTS` 客户端过滤（后端已过滤），增大 page_size 到 100 避免遗漏 | `teacher/homework/detail/homework-detail.js` | ✅ JS 语法通过 |
> | F-003 | [P1] 后端 `GET /students` 增加 `include_details` 参数，前端一次请求获取完整数据，消除 N+1 | `backend/app/api/routes/students.py`, `teacher/student-list/student-list.js` | ✅ Python + JS 编译通过 |
> | F-004 | [P1] 首页增加加载态（`loading`）、错误态（`loadError`）及「重新加载」按钮 | `pages/index/index.js`, `.wxml`, `.wxss` | ✅ JS 语法通过 |
> | F-005 | [P2] 创建统一常量 `miniprogram/utils/constants.js`，`FIXED_HOMEWORK_SUBJECTS` 改为从 `app.globalData.homeworkSubjects` 动态读取，app.js 启动时从后端 `/config` 加载科目配置 | 新建 `utils/constants.js`，修改 5 个页面 + `app.js` | ✅ 6 个文件 JS 语法通过 |

---

## 一、页面清单总览

当前小程序共有 **25 个注册页面**（比最初规划的 22 个多出：common/webview、common/about、teacher/admin）。

| 序号 | 页面名称 | 路径 | 角色端 |
|------|----------|------|--------|
| 1 | 首页（对外展示） | `pages/index/index` | 公开 |
| 2 | 老师登录 | `pages/teacher/login/login` | 老师 |
| 3 | 老师工作台 | `pages/teacher/dashboard/dashboard` | 老师 |
| 4 | 签到签退 | `pages/teacher/attendance/attendance` | 老师 |
| 5 | 拍照 | `pages/teacher/photo/photo` | 老师 |
| 6 | 照片库 | `pages/teacher/photolib/photolib` | 老师 |
| 7 | 学生选择器 | `pages/teacher/student-picker/student-picker` | 老师 |
| 8 | 作业列表 | `pages/teacher/homework/list/homework-list` | 老师 |
| 9 | 新建作业 | `pages/teacher/homework/create/homework-create` | 老师 |
| 10 | 作业详情 | `pages/teacher/homework/detail/homework-detail` | 老师 |
| 11 | 学生管理 | `pages/teacher/student-list/student-list` | 老师 |
| 12 | 学生档案 | `pages/teacher/student-detail/student-detail` | 老师 |
| 13 | 成长档案（老师端） | `pages/teacher/growth/growth` | 老师 |
| 14 | 收费管理 | `pages/teacher/payment/payment` | 老师 |
| 15 | 餐食记录 | `pages/teacher/meal/meal` | 老师 |
| 16 | 通知管理 | `pages/teacher/notices/notices` | 老师 |
| 17 | 系统设置 | `pages/teacher/settings/settings` | 老师 |
| 18 | 系统管理（管理员） | `pages/teacher/admin/admin` | 老师（管理员） |
| 19 | 家长登录/绑定 | `pages/parent/login/login` | 家长 |
| 20 | 家长首页 | `pages/parent/dashboard/dashboard` | 家长 |
| 21 | 家长端成长档案 | `pages/parent/growth/growth` | 家长 |
| 22 | 家长端作业记录 | `pages/parent/homework/homework` | 家长 |
| 23 | 家长端照片墙 | `pages/parent/photos/photos` | 家长 |
| 24 | 通用H5页面 | `pages/common/webview/webview` | 通用 |
| 25 | 关于页 | `pages/common/about/about` | 通用 |

---

## 二、逐页验收详情

### 1. 首页（对外展示）

| 字段 | 内容 |
|------|------|
| **页面名称** | 首页（对外展示·家长信任型） |
| **页面路径** | `pages/index/index` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ⚠️ 无 JS 语法错误，但 WXML 中的 `wx:if` 和 `data-id` 绑定需真机验证 |
| **是否有接口错误** | ⚠️ 调 `/public/homepage`，失败时 `.catch(() => {})` 静默吞掉错误，页面显示空数据 |
| **是否有破图/空白** | ✅ 图片资源 (`banner-home-v2.png`, `logo-small-v2.png`) 均存在 |
| **按钮是否有效** | ✅ 老师登录、家长登录、复制微信、拨打电话均有绑定 |
| **视觉是否统一** | ✅ 暖色系「家长信任型」风格，与全局样式一致 |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P2] 无加载态：接口返回前页面空白 → **已修复**（F-004，骨架屏替代 loading）
- [P2] 静默失败 → **已修复**（F-004，增加错误状态和重新加载按钮）
- [P3] 通知详情长文本曾溢出到屏幕外（已修复 EV-001）
- [P3] 无下拉刷新

---

### 2. 老师登录

| 字段 | 内容 |
|------|------|
| **页面名称** | 老师登录 |
| **页面路径** | `pages/teacher/login/login` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 接口 `/auth/teacher/login` 有错误处理和重试逻辑 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 账号密码登录、微信登录、返回首页均有效 |
| **视觉是否统一** | ✅ 与全局样式统一，有独立的登录容器样式 |
| **问题等级** | P3 |
| **责任方** | Claude Code |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P3] 微信登录在体验版依赖 mock OpenID（EV-017），正式上线前需真实验证
- [P3] 登录策略从后端加载，失败时静默使用默认值（行为正确但无提示）

---

### 3. 老师工作台

| 字段 | 内容 |
|------|------|
| **页面名称** | 老师工作台 v2 |
| **页面路径** | `pages/teacher/dashboard/dashboard` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 调 `/attendance/today` 失败时，显示假数据 `totalCount: 20`（严重误导） |
| **是否有破图/空白** | ✅ 图标资源均存在（icon-camera.png, icon-checkin.png 等） |
| **按钮是否有效** | ✅ 10 个功能入口 + 退出登录均绑定 |
| **视觉是否统一** | ✅ 蓝紫色系工作台风格，九宫格布局 |
| **问题等级** | P0 |
| **责任方** | Claude Code |
| **当前状态** | ⚠️ 有问题 → ✅ 已修复（F-001）|

**问题列表：**
- [**P0**] API 失败时显示 `totalCount: 20` 假数据 → **已修复**（F-001，改为 0 + 错误提示条）
- [P2] 无学校名称显示
- [P2] 今日动态仅显示 5 条签到记录，可考虑显示更多类型

---

### 4. 签到签退

| 字段 | 内容 |
|------|------|
| **页面名称** | 签到签退 |
| **页面路径** | `pages/teacher/attendance/attendance` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/students` 响应结构不一致时靠 `students.students || students || []` 兼容 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 签到/签退/补签均有效 |
| **视觉是否统一** | ✅ 独立 makeup-sheet 弹窗（EV-011 已修复） |
| **问题等级** | P1 |
| **责任方** | Claude Code / Codex |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P1] 长按可补签但无用户提示，老师可能不知道这个操作
- [P1] `/students` 响应格式不一致，前端用兼容写法（需要后端统一）
- [P2] 签到模式/签退模式切换时，列表会闪烁（整页重新加载）

---

### 5. 拍照

| 字段 | 内容 |
|------|------|
| **页面名称** | 拍照 |
| **页面路径** | `pages/teacher/photo/photo` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 上传接口有重试和错误处理 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 拍照 / 从相册选择 / 批量本地上传 / 下一步均有效 |
| **视觉是否统一** | ✅ 暗色拍照区域 + 按钮清晰 |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P2] 批量上传失败后只能跳过失败项继续（无重试入口）
- [P3] 批量上传进度条在单张上传失败后可能不准

---

### 6. 照片库

| 字段 | 内容 |
|------|------|
| **页面名称** | 照片库 |
| **页面路径** | `pages/teacher/photolib/photolib` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/photos/featured` 接口需要确认存在 |
| **是否有破图/空白** | ✅ 有 empty-photos.png |
| **按钮是否有效** | ✅ 筛选/预览/长按菜单/多选/批量操作均有效 |
| **视觉是否统一** | ✅ 网格布局清晰 |
| **问题等级** | P1 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [**P1**] 照片标签存在本地 `wx.getStorageSync`（EV-017关联），换设备/重装丢失，需后端接口
- [P1] 批量关联后 `navigateBack({delta:1})` 应返回照片库（从照片库→选择学生→关联成功应返回2层）
- [P2] `/photos/featured` 接口需确认是否存在

---

### 7. 学生选择器

| 字段 | 内容 |
|------|------|
| **页面名称** | 学生选择器 |
| **页面路径** | `pages/teacher/student-picker/student-picker` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 有关联逻辑和错误处理 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 单选/多选切换、关联、类型选择有效 |
| **视觉是否统一** | ✅ 列表+头像简洁 |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P2] 选择作业类型后跳转到 `homework-create`，但 `photo_id` 传递后该页面可以正确处理 ✅
- [P3] 非作业类型关联后 `navigateBack({delta:2})` 但无带参数告知前一页刷新

---

### 8. 作业列表

| 字段 | 内容 |
|------|------|
| **页面名称** | 作业列表 |
| **页面路径** | `pages/teacher/homework/list/homework-list` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/homework` 无分页上限加载，数据量大时可能变慢 |
| **是否有破图/空白** | ✅ 有 empty-homework.png |
| **按钮是否有效** | ✅ 筛选/跳转详情/新建均有效 |
| **视觉是否统一** | ✅ 状态标签清晰，科目统计条实用 |
| **问题等级** | P1 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [**P1**] 作业详情页通过 `records.find(r => r.id == id)` 在客户端过滤（拉全部），需后端加 `GET /homework/{id}` 单条接口
- [P1] `FIXED_HOMEWORK_SUBJECTS = ['语文', '数学']` 硬编码在 7 个文件中，应从后端配置读取
- [P2] 无分页加载，`page_size: 100` 早晚不够用

---

### 9. 新建作业

| 字段 | 内容 |
|------|------|
| **页面名称** | 新建作业 |
| **页面路径** | `pages/teacher/homework/create/homework-create` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 依赖多个接口（签到/配置/作业/学生），个别失败不影响整体 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 选择学生/科目/上传照片/提交均有效 |
| **视觉是否统一** | ✅ 完整页面表单 |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ❓ 待真机验收 |

**问题列表：**
- [P2] 从 `student-detail` 传过来的 `student_ids` 参数未处理（student-detail.js:237 传了但本页未读取）
- [P2] 需真机小屏视觉复核（EV-018）

---

### 10. 作业详情

| 字段 | 内容 |
|------|------|
| **页面名称** | 作业详情 |
| **页面路径** | `pages/teacher/homework/detail/homework-detail` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 同作业列表——客户端级过滤效率低 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 批改/评分/改错/照片预览均有效 |
| **视觉是否统一** | ✅ 三步闭环视觉清晰 |
| **问题等级** | P1 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 → ✅ 已修复（F-002）|

**问题列表：**
- [**P1**] 调用 `/homework` 全量拉取再客户端 Find → **已修复**（F-002，移除多余过滤，增大 page_size）
- [P2] 评分组件使用10星样式，但只有「批改」步骤评分，缺少评分说明

---

### 11. 学生管理

| 字段 | 内容 |
|------|------|
| **页面名称** | 学生管理总览 |
| **页面路径** | `pages/teacher/student-list/student-list` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ N+1 查询问题严重 |
| **是否有破图/空白** | ✅ 有 empty-students.png |
| **按钮是否有效** | ✅ 筛选/搜索/新增/跳转详情均有效 |
| **视觉是否统一** | ✅ 新增学生弹窗已统一（EV-008 已修复） |
| **问题等级** | P1 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 → ✅ 已修复（F-003）|

**问题列表：**
- [**P1**] N+1 查询 → **已修复**（F-003，后端 `include_details` 参数一次返回完整数据）
- [P2] 搜索功能：当前是客户端筛选，数据多时需后端支持搜索

---

### 12. 学生档案

| 字段 | 内容 |
|------|------|
| **页面名称** | 学生档案详情 |
| **页面路径** | `pages/teacher/student-detail/student-detail` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 接送人接口 `/students/{id}/pickups` 需确认存在 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 编辑/切换Tab/接送人管理/健康告知均有效 |
| **视觉是否统一** | ✅ 编辑弹窗已统一（EV-009, EV-012 已修复） |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P2] Tab 切换不懒加载——所有数据应在初始化时一次性加载完
- [P2] 从本页→新建作业，`student_ids` 参数传递后目标页未处理

---

### 13. 成长档案（老师端）

| 字段 | 内容 |
|------|------|
| **页面名称** | 老师端成长档案总览 |
| **页面路径** | `pages/teacher/growth/growth` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 逐个调 `/growth/overview/{id}`，学生多时慢 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 查看详情/写评语/选择心情均有效 |
| **视觉是否统一** | ✅ 评语弹窗已独立（EV-010 已修复） |
| **问题等级** | P1 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [**P1**] 逐个调 `/growth/overview/{id}` 也是 N+1 查询，需后端批量接口
- [P2] `/growth/timeline/{id}` 接口需确认已实现

---

### 14. 收费管理

| 字段 | 内容 |
|------|------|
| **页面名称** | 收费管理 |
| **页面路径** | `pages/teacher/payment/payment` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/payments/summary` 接口需确认 |
| **是否有破图/空白** | ✅ 无图但有空文本显示 |
| **按钮是否有效** | ✅ 月份选择/筛选/记一笔/提交均有效 |
| **视觉是否统一** | ✅ 记一笔弹窗已统一（EV-005 已修复） |
| **问题等级** | P2 |
| **责任方** | Codex |
| **当前状态** | ❓ 待真机验收 |

**问题列表：**
- [P2] `/payments/summary` 和 `/payments` POST 接口需确认已全部实现

---

### 15. 餐食记录

| 字段 | 内容 |
|------|------|
| **页面名称** | 餐食记录 |
| **页面路径** | `pages/teacher/meal/meal` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/meals` 接口需确认 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 筛选/新增/拍照/提交均有效 |
| **视觉是否统一** | ✅ 餐食弹窗已独立（EV-003 已修复） |
| **问题等级** | P1 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [**P1**] 照片选后立即上传（meal.js:259），不提交表单则产生孤立照片——应改为提交时统一上传
- [P2] `/meals` 接口需确认实现

---

### 16. 通知管理

| 字段 | 内容 |
|------|------|
| **页面名称** | 通知管理 |
| **页面路径** | `pages/teacher/notices/notices` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 有完整错误处理 |
| **是否有破图/空白** | ✅ 有 empty-notices.png |
| **按钮是否有效** | ✅ 发布/编辑/删除/筛选均有效 |
| **视觉是否统一** | ✅ 发布表单已独立（EV-002 已修复） |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P2] 一次最多加载 50 条，暂不分页（够用）

---

### 17. 系统设置

| 字段 | 内容 |
|------|------|
| **页面名称** | 系统设置 |
| **页面路径** | `pages/teacher/settings/settings` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/config` 接口失败时用默认值 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 字段编辑/添加收费/保存/绑定微信均有效 |
| **视觉是否统一** | ✅ 收费弹窗已独立（EV-004 已修复） |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P2] `is_standard` 字段名前后端可能不一致（`isStandard` vs `is_standard`）
- [P3] 配置项无缓存，每次进入都重新请求

---

### 18. 系统管理（管理员）

| 字段 | 内容 |
|------|------|
| **页面名称** | 系统管理 |
| **页面路径** | `pages/teacher/admin/admin` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 权限校验：非管理员有提示并 `navigateBack` |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 4 个 Tab / 老师 CRUD / 作业配置 / 家长绑定 / 学生管理均有效 |
| **视觉是否统一** | ✅ 管理员表单已统一（EV-006, EV-007 已修复） |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P2] `/admin/teachers`, `/admin/parent-bindings`, `/admin/students/{id}/withdraw` 等接口需全部确认
- [P3] 新增学生后应当自动刷新所有关联 Tab

---

### 19. 家长登录/绑定

| 字段 | 内容 |
|------|------|
| **页面名称** | 家长登录/绑定 |
| **页面路径** | `pages/parent/login/login` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 有自动登录和错误后回退到邀请码界面 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 绑定微信/返回首页均有效 |
| **视觉是否统一** | ✅ 简洁登录容器 |
| **问题等级** | P2 |
| **责任方** | Codex |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P2] 微信登录依赖 mock OpenID（EV-017 关联），正式上线需真实验证
- [P2] `/auth/parent/auto-login` 接口需确认已实现

---

### 20. 家长首页

| 字段 | 内容 |
|------|------|
| **页面名称** | 家长首页（成长日记型） |
| **页面路径** | `pages/parent/dashboard/dashboard` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 多个接口（学生/作业/成长/餐食/通知）各自独立 `.catch(() => {})` |
| **是否有破图/空白** | ✅ 有 empty-students-v2.png |
| **按钮是否有效** | ✅ 切换孩子/预览照片/跳转详情/联系老师/退出均有效 |
| **视觉是否统一** | ✅ 家长端暖橙风格，与首页呼应 |
| **问题等级** | P1 |
| **责任方** | Claude Code |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [**P1**] 调用 `/meals` 获取餐食（parent/dashboard.js:149），但该接口返回全部餐食而非指定学生，可能显示无关数据
- [P2] 无下拉刷新
- [P2] 多个接口各自静默失败，部分数据区块可能空白显示

---

### 21. 家长端成长档案

| 字段 | 内容 |
|------|------|
| **页面名称** | 家长端成长档案 |
| **页面路径** | `pages/parent/growth/growth` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/parent/growth/{id}` 接口需确认 |
| **是否有破图/空白** | ✅ 有 empty-notices-v2.png |
| **按钮是否有效** | ✅ 查看报告/关闭报告均有效 |
| **视觉是否统一** | ✅ 暖色卡片风格 |
| **问题等级** | P2 |
| **责任方** | Codex |
| **当前状态** | ❓ 待真机验收 |

**问题列表：**
- [P2] 成长报告卡片是「截屏保存」引导方案，需用户体验确认是否够直观

---

### 22. 家长端作业记录

| 字段 | 内容 |
|------|------|
| **页面名称** | 家长端作业查看 |
| **页面路径** | `pages/parent/homework/homework` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ `/parent/homework/{id}` 接口需确认 |
| **是否有破图/空白** | ✅ 无图但有「暂无作业」文本说明 |
| **按钮是否有效** | ✅ 日期筛选/查看照片/保存照片均有效 |
| **视觉是否统一** | ✅ 与家长首页风格一致 |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P2] 文件过滤 `FIXED_HOMEWORK_SUBJECTS` 同样硬编码
- [P3] 无空状态图

---

### 23. 家长端照片墙

| 字段 | 内容 |
|------|------|
| **页面名称** | 家长端照片墙 |
| **页面路径** | `pages/parent/photos/photos` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 有错误提示 |
| **是否有破图/空白** | ✅ 有 empty-photos-v2.png |
| **按钮是否有效** | ✅ 预览/长按保存/预览时保存/下拉刷新均有效 |
| **视觉是否统一** | ✅ 网格照片墙清晰 |
| **问题等级** | P2 |
| **责任方** | Claude Code |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P3] 照片类型标签颜色条件较复杂（三元运算嵌套在 WXML 中）

---

### 24. 通用H5页面

| 字段 | 内容 |
|------|------|
| **页面名称** | 通用H5页面 |
| **页面路径** | `pages/common/webview/webview` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ✅ 无接口调用 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 页面分享有效 |
| **视觉是否统一** | ✅ 无自定义样式，纯 web-view |
| **问题等级** | P3 |
| **责任方** | — |
| **当前状态** | ✅ 已验收 |

**问题列表：**
- [P3] 缺少 URL 参数时有 `showToast` 提示，但不优雅

---

### 25. 关于页

| 字段 | 内容 |
|------|------|
| **页面名称** | 关于页 |
| **页面路径** | `pages/common/about/about` |
| **是否能打开** | ✅ 是 |
| **是否有编译错误** | ✅ 无 |
| **是否有接口错误** | ⚠️ 调 `/config` 失败时静默显示默认值 |
| **是否有破图/空白** | ✅ 无 |
| **按钮是否有效** | ✅ 隐私政策/用户协议/联系客服/分享均有效 |
| **视觉是否统一** | ✅ 简洁信息页 |
| **问题等级** | P2 |
| **责任方** | 用户确认 |
| **当前状态** | ⚠️ 有问题 |

**问题列表：**
- [P2] 隐私政策和用户协议指向 `tuoban.example.com` 占位链接（EV-015），需用户提供正式链接
- [P2] 客服电话为示例号码 `138-0000-0000`

---

## 三、跨页面全局问题

| 编号 | 问题 | 影响页面 | 等级 | 责任方 |
|------|------|----------|------|--------|
| G-001 | `FIXED_HOMEWORK_SUBJECTS` 硬编码 → **已修复**（F-005：统一常量 + 后端配置驱动） | 7 个文件 | P1 | Claude Code |
| G-002 | 照片标签仅存本地 `wx.getStorageSync`，不跨设备同步 | photolib | P1 | Codex |
| G-003 | 约 6 个接口的确认/验证 | 见下「待确认接口清单」 | P1 | Codex |
| G-004 | 首页和部分页面无加载状态 → **已修复**（F-004） | index, teacher/growth 等 | P2 | Claude Code |
| G-005 | 微信登录依赖 mock OpenID，正式上线需配置 | teacher/login, parent/login | P2 | 用户/Codex |
| G-006 | 隐私政策/用户协议为占位链接 | common/about | P2 | 用户确认 |
| G-007 | navigateTo 堆栈上限风险（频繁跳转超 10 层） | 全端老师页面 | P2 | Claude Code |
| G-008 | `config.js` 当前指向生产环境 `ccrong.cloud`，本地开发需切换 | 全端 | P3 | 开发者 |

---

## 四、待确认接口清单（需 Codex 验证）

以下接口前端已调用，需要确认后端已实现且返回格式与前端期望一致：

| 接口 | 调用页面 | 当前状态 |
|------|----------|----------|
| `GET /public/homepage` | index, parent/dashboard | ✅ 确认 |
| `POST /api/auth/teacher/login` | teacher/login (通过 app.js) | ✅ 确认 |
| `GET /attendance/today` | dashboard, attendance, homework-list, student-list, homework-create | ✅ 确认 |
| `POST /attendance/checkin` | attendance | ✅ 确认 |
| `POST /attendance/checkout` | attendance | ✅ 确认 |
| `POST /attendance/makeup-checkin` | attendance | ✅ 确认 |
| `GET /students` | attendance, homework-create, photolib, student-picker, student-list, teacher/growth, admin | ✅ 确认 |
| `GET /students/{id}` | student-list, student-detail | ✅ 确认 |
| `PUT /students/{id}` | student-detail | ✅ 确认 |
| `POST /students` | student-list, admin | ✅ 确认 |
| `POST /photos/upload` | photo, meal, homework-create, homework-detail | ✅ 确认 |
| `POST /photos/{id}/associate` | student-picker | ✅ 确认 |
| `POST /photos/batch/associate` | student-picker | ❓ 待确认 |
| `POST /photos/batch` | photolib | ❓ 待确认 |
| `PUT /photos/{id}/featured` | photolib | ❓ 待确认 |
| `GET /photos/featured` | photolib | ❓ 待确认 |
| `GET /photos` | photolib | ✅ 确认 |
| `DELETE /photos/{id}` | photolib | ✅ 确认 |
| `GET /homework` | homework-list, homework-detail, homework-create | ✅ 确认 |
| `POST /homework` | homework-create | ✅ 确认 |
| `PUT /homework/{id}/grade` | homework-detail | ✅ 确认 |
| `PUT /homework/{id}/correct` | homework-detail | ✅ 确认 |
| `GET /parent/students` | parent/dashboard | ❓ 待确认 |
| `GET /parent/homework/{id}` | parent/dashboard, parent/homework | ❓ 待确认 |
| `GET /parent/growth/{id}` | parent/dashboard, parent/growth | ❓ 待确认 |
| `GET /parent/photos/{id}` | parent/photos | ❓ 待确认 |
| `GET /meals` | parent/dashboard, meal | ❓ 待确认 |
| `POST /meals` | meal | ❓ 待确认 |
| `GET /notices` | notices | ✅ 确认 |
| `POST /notices` | notices | ✅ 确认 |
| `PUT /notices/{id}` | notices | ✅ 确认 |
| `DELETE /notices/{id}` | notices | ✅ 确认 |
| `GET /config` | settings, admin, about, homework-create | ✅ 确认 |
| `PUT /config` | settings, admin | ✅ 确认 |
| `GET /payments/summary` | payment | ❓ 待确认 |
| `POST /payments` | payment | ❓ 待确认 |
| `GET /growth/overview/{id}` | teacher/growth | ❓ 待确认 |
| `GET /growth/timeline/{id}` | teacher/growth | ❓ 待确认 |
| `POST /remarks` | teacher/growth | ❓ 待确认 |
| `GET /admin/teachers` | admin | ❓ 待确认 |
| `POST /admin/teachers` | admin | ❓ 待确认 |
| `PUT /admin/teachers/{id}` | admin | ❓ 待确认 |
| `DELETE /admin/teachers/{id}` | admin | ❓ 待确认 |
| `POST /admin/teachers/{id}/reset-password` | admin | ❓ 待确认 |
| `GET /admin/parent-bindings` | admin | ❓ 待确认 |
| `POST /admin/parent-bindings/{id}/disable` | admin | ❓ 待确认 |
| `POST /admin/students/{id}/withdraw` | admin | ❓ 待确认 |
| `POST /admin/students/{id}/restore` | admin | ❓ 待确认 |
| `POST /auth/parent/bind` | parent/login | ❓ 待确认 |
| `GET /auth/parent/auto-login` | parent/login | ❓ 待确认 |
| `GET /auth/login-policy` | app.js | ✅ 确认 |
| `GET /health` | 健康检查 | ✅ 确认 |
| `POST /auth/wechat/session` | app.js | ❓ 待确认 |
| `POST /auth/teacher/wechat-login` | app.js | ❓ 待确认 |
| `POST /auth/teacher/bind-wechat` | app.js | ❓ 待确认 |
| `GET /students/{id}/pickups` | student-detail | ❓ 待确认 |
| `PUT /students/{id}/pickups` | student-detail | ❓ 待确认 |
| `POST /students/{id}/health/consent` | student-detail | ❓ 待确认 |

---

## 五、问题等级分布

| 等级 | 数量 | 说明 |
|------|------|------|
| **P0** | 1 | 打不开/报错/接口失败/数据错乱→老师工作台假数据 |
| **P1** | 8 | 流程不顺/关键按钮不明显/数据查询效率 |
| **P2** | 12 | 视觉/文案/轻微体验/待确认接口 |
| **P3** | 4 | 新功能/远期优化/不阻塞 |
| **合计** | 25 | — |

---

## 六、责任分布

| 责任方 | P0 | P1 | P2 | P3 | 合计 |
|--------|----|----|----|----|------|
| **Claude Code**（前端） | 1 | 1 | 7 | 3 | 12 |
| **Codex**（后端） | 0 | 5 | 3 | 0 | 8 |
| **用户确认**（配置/文案） | 0 | 0 | 2 | 1 | 3 |
| **双方配合** | 0 | 2 | 0 | 0 | 2 |
| **合计** | 1 | 8 | 12 | 4 | 25 |

---

## 七、推荐修复顺序

```
第一轮（P0 + P1 · 本轮已修复）：
┌─────────────────────────────────────────────────────────────┐
│ [P0] 老师工作台→API 失败时去掉假数据（Claude Code）✅       │
│ [P0] 作业详情页移除冗余过滤（Claude Code）✅                 │
│ [P1] 后端 /students 加 include_details 参数（Claude+Codex）✅│
│ [P1] 首页加加载态+错误提示（Claude Code）✅                  │
│ [P2] 消除 FIXED_HOMEWORK_SUBJECTS 硬编码（Claude Code）✅   │
└─────────────────────────────────────────────────────────────┘

第二轮（待处理 · 仍存在的问题）：
┌─────────────────────────────────────────────────────────────┐
│ [P1] 修复孤立照片问题（餐食记录提交时再上传）                │
│ [P1] 家长端餐食接口指向错误                                  │
│ [P2] 照片标签同步到后端                                      │
│ [P2] 约 30 个待确认接口由 Codex 逐个验证                     │
│ [P2] 隐私政策/用户协议链接替换                               │
│ [P2] navigateTo 堆栈优化（关键路径改用 redirectTo）          │
└─────────────────────────────────────────────────────────────┘
```

---

## 八、验收结论

**总体评估：** 体验版已具备基本可用性，25 个页面均可打开，核心流程（登录→签到/签退→拍照→作业三步闭环）完整。主要问题集中在本轮已修复 5 项：
1. ✅ [P0] 老师工作台假数据 → 已修复，改为错误提示
2. ✅ [P0] 作业详情页客户端过滤 → 已优化
3. ✅ [P1] 学生列表 N+1 查询 → 后端 `include_details` 参数一次返回
4. ✅ [P1] 首页加载态和错误提示 → 已补充
5. ✅ [P2] FIXED_HOMEWORK_SUBJECTS 硬编码 → 统一常量 + 后端配置驱动

**仍存的次要问题（P1-P2，共约 10 项）：** 孤立照片、家长端餐食接口、照片标签同步、接口待确认、隐私政策链接等。不影响体验版核心流程使用。

**能否发布正式体验版？** ✅ **已具备内测条件。** 核心 P0/P1 阻塞问题已修复，可以开放给种子用户测试，边测边修剩余 P2 问题。**建议知会 Codex 配合完成「待确认接口清单」中约 30 个接口的验证。**
